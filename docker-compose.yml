services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: pitchside
      POSTGRES_PASSWORD: pitchside
      POSTGRES_DB: pitchside
    ports:
      - "5433:5432"
    volumes:
      - pitchside_pg_data:/var/lib/postgresql/data

volumes:
  pitchside_pg_data:
